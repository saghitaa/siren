name: siren_application
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  google_fonts: ^6.2.1 
  cupertino_icons: ^1.0.8

  # === Firebase & Backend ===
  firebase_core: ^2.24.0
  cloud_firestore: ^4.13.0
  firebase_messaging: ^14.7.0
  cloud_functions: ^4.6.0
  firebase_auth: ^4.15.0
  
  # === Mobile utilities ===
  geolocator: ^10.1.0
  audioplayers: ^5.2.0
  url_launcher: ^6.3.0
  sqflite: ^2.3.0  # Untuk cache offline (opsional)
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/sounds/